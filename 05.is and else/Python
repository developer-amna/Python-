"""
       if statement:
                if statement is used to test a specific condition. If the condition evaluates to True, the block of code within the if statement is executed. If the condition is False, the block of code is skipped.
 if-else statement:
                 The if-else statement is an extension of the if statement. It allows you to specify an alternative block of code to execute when the condition is False. If the condition is True, the code within the if block is executed; otherwise, the code within the else block is executed.
                 """
                 
               #  Program # 1:
                 
num = int(input("Enter a number: "))  # Taking input from user
if num >=10 and num <=50:  # Checking if the number is between 10 and 50
    print("The number is between 10 and 50.")  # If True, print this message
else:
    print("The number is outside the range of 10 to 50.")  # If False, print this message
    
#Output:
#Enter a number: 25
#The number is between 10 and 50.

#Program # 2:

age = int (input("Enter your age: "))  # Taking input from user
citizen = input("Are you citizen of Pakistan? (yes/no): ")  # Taking citizenship input and normalizing it

if age >=18 and citizen . lower() == "yes":  # Checking if age is 18 or older and if the user is a citizen
    print("You are eligible to vote.")  # If both conditions are True, print this message
else:
    print("You are not eligible to vote.")
# If either condition is False, print this message

#Output:
#Enter your age: 20
#Are you citizen of Pakistan? (yes/no): yes
#You are eligible to vote.

#program # 3:

password = input("Enter your password: ")  # Taking password input from user
if len(password)>=8 and any(ch.isdigit() for ch in password) and any(char.isupper() for char in password):  # Checking password criteria
    print("Strong password.")  # If all conditions are True, print this message   
else:
    print("Weak password.")
    # If any condition is False, print this message
     
#Output:
#Enter your password: 123
#Weak password.

#program # 4:

maths = int(input("Enter Maths marks: "))  # Taking Maths marks input
science = int(input("Enter Science marks: "))  # Taking Science marks input

if maths >=40 and science >=40:  # Checking if both marks are 40 or above
    print(You passes in both subjects.)  # If True, print this message
else:
    print("You failed")
    
    #Output:
#Enter Maths marks: 20
#Enter Science marks: 30
#You failed

#program # 5:

total = float(input("Enter total shopping amount: "))
member = input("Are you a member? (yes/no): ")
if total >= 5000 and member.lower() == "yes":
    print("You get a 20% discount.")
else:
    print("No discount available.")
    
#Output:
#Enter total shopping amount: 30
#Are you a member? (yes/no): no
#No discount available.

#program # 6:

total = float(input("Enter total shopping amount: "))
member = input("Are you a member? (yes/no): ")
if total >= 5000 and member.lower() == "yes":
    print("You get a 20% discount.")
else:
    print("No discount available.")
    
#Output:
#Enter total shopping amount: 60
#Are you a member? (yes/no): yes
#You get a 20% discount.

